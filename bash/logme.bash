for i in {0..300}
do
        logger -p local6.alert "ip=8.8.8.8 [`date`] test from rsyslog udp ----------------------------------------------------------------------------------------------------------------------------------------------- `cat /dev/urandom | tr -dc _A-Z-a-z-0-9 | head -c500`"
        logger -p local7.alert "ip=8.8.8.8 [`date`] test from rsyslog tcp ----------------------------------------------------------------------------------------------------------------------------------------------- `cat /dev/urandom | tr -dc _A-Z-a-z-0-9 | head -c500`"
        sleep 1
done
